import { ChangePasswordFormDTO } from './change-password-form-dto';

describe('ChangePasswordFormDTO', () => {
  it('should create an instance', () => {
    expect(new ChangePasswordFormDTO()).toBeTruthy();
  });
});
